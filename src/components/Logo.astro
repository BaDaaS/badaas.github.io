---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span class:list={["logo-text", className]}>BaDaaS</span>

<style>
  .logo-text {
    display: inline;
    color: var(--accent-color);
  }
</style>
